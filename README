#execute following commands

npm install
gulp build
# For test cases
#./node_modules/karma/bin/karma  start ../websdk/app/test/jasmine/websdk_test.conf.js 

gulp test

#For creating documentation first command create documentations with basic template and the second one will create ink-docstrap template
./node_modules/.bin/jsdoc app/src/sdk.js or 
./node_modules/.bin/jsdoc -c jsdoc.conf -t ./node_modules/ink-docstrap/template

For Accesstoken use SDK as
 
              var inputs={};
              inputs.client_id="";
              inputs.client_secret="";
              inputs.developer_id="";
              
              KnurldSDK.auth.accesstoken(inputs, successCallback, errorCallBack);
